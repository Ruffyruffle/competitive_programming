#include <bits/stdc++.h>


#define s(x) #x
using namespace std;

int main(){
    string zero;
    string a = s(+);
    string b = s(!);
    b[zero.length()]--;
    a[zero.length()]++;
    cout<<s(Hello)<<a<<b<<s(World!);



}
